<p class="timestamps update-in-place" id="timestamps">
  Current time: <span class="current-time"><%= Time.now.strftime('%Y-%m-%d %H:%M:%S UTC') %></span>
  <% if @last_updated_at %>
    (Last updated <%= format_time @last_updated_at, time_only: true %>)
  <% end %>
</p>
