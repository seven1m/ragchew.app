<!doctype html>
<html lang="en">
  <head>
    <title>NetLogger Web</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/typesafe.css">
    <style>
      span.Offline { color: #ccc; }
      em.highlight { font-style: normal; background-color: #ffc; }
      h2 { border-bottom: 3px double #999; }
      body { max-width: 100%; }
      .last-updated { font-size: smaller; font-style: italic; }
    </style>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
